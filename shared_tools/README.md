usage for wrap-xml.perl:

    wrap-xml.perl Language(en/cs) SOURCE.sgm(the original sgm you use) OurSystemName < InputRawText > OUTPUT.sgm

usage for sgm_parser.py:

    python sgm_parser.py Input Output
